/*
  Challenges
  -----------
  1. Declare an array named `teaFlavors` that contains the strings `green tea`, `black tea`, and `oolong tea`.
      Access the first element of the array and store it in a variable named `firstTea`.
*/
  // challenge 1
  const teaFlavors = ["green tea", "black tea", "oolong tea"];
  const firstTea = teaFlavors[0];
  // console.log(`Your first tea choice is "${firstTea}"`);

/*  
  2. Declare an array named "cities" containing "New Delhi", "London", "Tokyo", "Paris", and "New York".
      Access the third element in the array and store it in a variable named "favoriteCity".
*/
  // challenge 2
  const cities = ["New Delhi", "London", "Tokyo", "Paris", "New York"];
  const favoriteCity = cities[2];
  // console.log(`My favorite city is "${favoriteCity}"`);

/*
  3. You have an array named "teaTypes" containing "herbal tea", "white tea", and "masala chai".
      Change the second element of the array to "jasmine tea".
*/
  // challenge 3
  const teaTypes = ["herbal tea", "white tea", "masala chai"];
  teaTypes[1] = "jasmine tea";
  console.log(teaTypes);